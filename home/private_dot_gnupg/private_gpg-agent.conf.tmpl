# https://github.com/drduh/YubiKey-Guide/blob/master/config/gpg-agent.conf
# https://www.gnupg.org/documentation/manuals/gnupg/Agent-Options.html
enable-ssh-support
ttyname $GPG_TTY
# long enough that one passphrase covers a burst of mbsync/vdirsyncer
# timers all missing pass-cache at once (after login or the daily
# expiry), instead of a pinentry per gpg call
default-cache-ttl 600
max-cache-ttl 3600
default-cache-ttl-ssh 60
max-cache-ttl-ssh 120
# Select a valid program path for PIN entry prompt
#pinentry-program /usr/bin/pinentry-curses
#pinentry-program /usr/bin/pinentry-gnome3
#pinentry-program /usr/bin/pinentry-tty
#pinentry-program /usr/bin/pinentry-x11
#pinentry-program /usr/local/bin/pinentry-curses
{{ if eq .chezmoi.os "darwin" -}}
pinentry-program /opt/homebrew/bin/pinentry-mac
{{ else -}}
pinentry-program /usr/bin/pinentry
{{ end -}}
# https://developers.yubico.com/PGP/SSH_authentication/Windows.html
#enable-win32-openssh-support
#log-file /tmp/gpg-agent.log
#debug-level basic
