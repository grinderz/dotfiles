#!/bin/sh
# pass against the work store. A script rather than a shell function
# so bash scripts, PassCmd hooks and systemd units can use it too.
PASSWORD_STORE_DIR=$HOME/sync/work/pass exec pass "$@"
