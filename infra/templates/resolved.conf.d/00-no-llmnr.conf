# LLMNR is a poisoning target and legacy (mDNS covers the use case).
# If Windows hosts must resolve this machine by name: set LLMNR=yes here
# and re-open port 5355 in group_data (nftables_open_udp/tcp).
[Resolve]
LLMNR=no
