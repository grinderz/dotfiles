# Keys only (ed25519-sk); passwords never travel over ssh, faillock
# stays a local-console concern. For sshd the FIRST obtained value wins
# and the Include sits at the top of sshd_config, so 10- beats both the
# 99-archlinux drop-in and anything in the main file.
PasswordAuthentication no
KbdInteractiveAuthentication no
