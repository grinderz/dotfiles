# Number-based retention only fills up on pacman transactions (snap-pac);
# hourly runs are mostly no-ops, and each real cleanup rewrites limine.conf
# and rsyncs both boot sticks. Daily is enough.
[Timer]
OnUnitActiveSec=
OnUnitActiveSec=1d
