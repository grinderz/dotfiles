# @local cannot be renamed, so per-host groups serve as --limit aliases
local = [("@local", {})]

tb_6_dock = [
    (
        "tb-6-dock2-mokin-3707",
        {
            "ssh_hostname": "dock2-mokin-3707",
            "systemd_network_wired_dock_match_name": "enx207bd2e36ccb",
        },
    ),
]
